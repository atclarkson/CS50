//mario program for CS50
//By Adam T Clarkson
//Prmpt user for height and recreate with # the end of level pyramid from super mario brothers

#include <cs50.h>
#include <stdio.h>

int main (void)
{
    // Setup some variables
    
    int height;
    
    printf("height: ");
    height = GetInt(height > 0 && height < 24);
    
    if(height > 0)
        {
            printf(" ")
        }
    else
    {
    }
    }
}